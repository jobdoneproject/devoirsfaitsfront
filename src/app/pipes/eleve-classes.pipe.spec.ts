import { EleveClassesPipe } from './eleve-classes.pipe';

describe('EleveClassesPipe', () => {
  it('create an instance', () => {
    const pipe = new EleveClassesPipe();
    expect(pipe).toBeTruthy();
  });
});
