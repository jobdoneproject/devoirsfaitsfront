import { NomDisponiblesPipe } from './nom-disponibles.pipe';

describe('NomDisponiblesPipe', () => {
  it('create an instance', () => {
    const pipe = new NomDisponiblesPipe();
    expect(pipe).toBeTruthy();
  });
});
