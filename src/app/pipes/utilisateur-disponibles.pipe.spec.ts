import { UtilisateurDisponiblesPipe } from './utilisateur-disponibles.pipe';

describe('UtilisateurDisponiblesPipe', () => {
  it('create an instance', () => {
    const pipe = new UtilisateurDisponiblesPipe();
    expect(pipe).toBeTruthy();
  });
});
