import { ElevesDisponiblesPipe } from './eleves-disponibles.pipe';

describe('ElevesDisponiblesPipe', () => {
  it('create an instance', () => {
    const pipe = new ElevesDisponiblesPipe();
    expect(pipe).toBeTruthy();
  });
});
